/* {} */
if (n >= 0) {
    x = Math.floor(n);
} else {
    x = Math.ceil(n);
}
if (n > 0) {
    x = Math.floor(n);
} else {
    x = Math.ceil(n);
}
if (n <= 0) {
    x = Math.ceil(n);
} else {
    x = Math.floor(n);
}
if (n < 0) {
    x = Math.ceil(n);
} else {
    x = Math.floor(n);
}
